# Entorno de las suites de prueba.
#
# El fichero de base de datos NO se declara aqui a proposito: lo decide
# `config/database.ts` a partir del entorno (ADR-0001), de modo que apuntar la
# suite a la base de desarrollo no sea una variable mal puesta sino algo
# imposible.
SESSION_DRIVER=memory

# Solo lo que rompa una prueba merece salir por consola, y `warn` no cumplia
# eso: `errores.spec.ts` provoca un 500 a proposito, el manejador lo registra
# en nivel `error` con el objeto completo, y cada ejecucion **verde** imprimia
# el `insert into users ... $scrypt$...` entero con su traza. En CI eso acaba
# en un log publico de Actions.
#
# `fatal` deja el canal mas ruidoso abierto por si algo se cae de verdad, y
# calla el error deliberado. Quien diagnostica una prueba mira su asercion, no
# el logger.
LOG_LEVEL=fatal

# La suite no puede depender del `.env` local de quien la ejecuta. ADR-0003
# invita a encender el volcado de depuracion a proposito, y quien lo haga se
# encontraria en rojo las pruebas de H-19 por su configuracion, no por el
# codigo. El valor por defecto del handler lo sigue vigilando el verificador.
DEBUG_HTTP_ERRORS=false

# La suite levanta su propio servidor HTTP, y `@japa/api-client` construye la
# URL con este mismo `PORT`. Heredado del `.env` era el 3333 del servidor de
# desarrollo: con `npm run dev` arriba, `node ace test` caia en el setup con
# `EADDRINUSE` y cero pruebas ejecutadas. Visto el 2026-09-12 al mutar un
# fichero con el servidor de desarrollo corriendo.
PORT=3334
